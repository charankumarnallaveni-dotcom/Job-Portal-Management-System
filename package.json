{
  "name": "job-portal-management-system",
  "version": "1.0.0",
  "private": true,
  "description": "Production-style full stack job portal with role based dashboards, applications, messaging, analytics, and AI-assisted matching.",
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace backend\" \"npm run dev --workspace frontend\"",
    "build": "npm run build --workspace frontend",
    "start": "npm run start --workspace backend",
    "seed": "npm run seed --workspace backend"
  },
  "workspaces": [
    "backend",
    "frontend"
  ],
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
